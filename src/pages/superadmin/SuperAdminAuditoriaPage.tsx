// src/pages/superadmin/SuperAdminAuditoriaPage.tsx
import { useEffect, useState } from "react";
import { superadminService } from "@/features/superadmin/api/superadminService";
import SuperAdminPagination from "@/features/superadmin/components/SuperAdminPagination";
import SuperAdminPageHeader from "./SuperAdminPageHeader";
export default function SuperAdminAuditoriaPage() { const [items, setItems] = useState<any[]>([]); const [total, setTotal] = useState(0); const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(20); const [error, setError] = useState<string | null>(null); useEffect(() => { superadminService.auditoria({ page, page_size: pageSize }).then((r) => { setItems(r.items); setTotal(r.total); }).catch((e) => setError(e.message)); }, [page, pageSize]); return <div><SuperAdminPageHeader title="Auditoría" subtitle="Eventos sensibles ejecutados en la plataforma." />{error && <div className="mb-5 rounded-2xl border border-red-200 bg-red-50 p-4 text-red-700">{error}</div>}<div className="rounded-3xl border bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Evento</th><th>Usuario</th><th>Empresa</th><th>IP</th><th>Fecha</th></tr></thead><tbody>{items.map((a) => <tr key={a.id} className="border-t"><td className="px-5 py-4 font-black">{a.evento}</td><td>{a.usuario_id || "—"}</td><td>{a.empresa_id || "—"}</td><td>{a.ip || "—"}</td><td>{a.creado_en ? new Date(a.creado_en).toLocaleString() : "—"}</td></tr>)}</tbody></table></div><SuperAdminPagination page={page} pageSize={pageSize} total={total} onPageChange={setPage} onPageSizeChange={(s) => { setPageSize(s); setPage(1); }} /></div></div>; }
