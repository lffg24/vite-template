// src/pages/superadmin/SuperAdminRolesPermisosPage.tsx
import { useEffect, useState } from "react";
import { superadminService } from "@/features/superadmin/api/superadminService";
import SuperAdminPageHeader from "./SuperAdminPageHeader";
export default function SuperAdminRolesPermisosPage() { const [data, setData] = useState<any>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { superadminService.rolesPermisos().then(setData).catch((e) => setError(e.message)); }, []); return <div><SuperAdminPageHeader title="Roles y permisos" subtitle="Base RBAC para autorización granular de plataforma." />{error && <div className="mb-5 rounded-2xl border border-red-200 bg-red-50 p-4 text-red-700">{error}</div>}<div className="grid gap-5 xl:grid-cols-2"><div className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Roles</h2><div className="mt-4 space-y-2">{(data?.roles || []).map((r: any) => <div key={r.id || r.codigo} className="rounded-2xl border p-4"><b>{r.codigo}</b><p className="text-sm text-slate-500">{r.nombre}</p></div>)}</div></div><div className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Permisos</h2><div className="mt-4 grid gap-2">{(data?.permisos || []).map((p: any) => <div key={p.id || p.codigo} className="rounded-xl bg-slate-50 px-3 py-2 text-sm font-bold">{p.codigo}</div>)}</div></div></div></div>; }
